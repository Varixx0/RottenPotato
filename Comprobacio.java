public class Comprobacio {
    
    public boolean comprobarValoracion(int posi){
        boolean bien = false;

        if (RottenApp.Peliculas.get(posi).getValoracion() ) {
            
        }

        return bien;
    }
}
